# Block common probes (valid)
/wp-admin*      404
/wp-login*      404
/.env           404

# Block extensions using regex (valid)
/(.*)\.php$     404
/(.*)\.html$    404
/(.*)\.htm$     404

# React SPA fallback (MUST be last)
/*              /index.html   200